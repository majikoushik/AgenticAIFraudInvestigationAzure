Fix only the remaining open review issues in this repository.

Repository:
C:\Koushik\AgenticAIFraudInvestigationAzure

Rules:
- Do not add real Azure secrets, credentials, customer data, or external service calls.
- Do not implement real Azure deployment unless explicitly requested.
- Do not revert unrelated user work.
- Keep this repository local-first and portfolio-safe.
- Be careful with synthetic data files: do not delete user-created data unless it is clearly generated by validation and safe to clean.
- Use existing project style and keep changes small.

Open issue 1: Pipeline documentation and YAML are inconsistent

Current problem:
- pipelines/README.md says validation uses offline structural validation with `az bicep build`.
- But pipelines/azure-pipelines.yml currently uses `az deployment sub validate`.
- pipelines/README.md says deployment stages are placeholders.
- But `deploy_infra_dev` in azure-pipelines.yml still runs a real `az deployment sub create`.

Required fix:
Make the pipeline story internally consistent and portfolio-safe.

Preferred approach:
- Keep this as a validation-first/template pipeline.
- Do not run real deployment commands by default.
- Replace the real `az deployment sub create` in `deploy_infra_dev` with an explicit placeholder echo, or rename that stage to clearly indicate it is a template only.
- Keep Bicep validation as either:
  Option A: online Azure validation using `az deployment sub validate`, and update pipelines/README.md to say that, or
  Option B: offline structural validation using `az bicep build`, and update azure-pipelines.yml to use that.
- Choose the cleaner option for a portfolio repo. Prefer no real deployment by default.

Acceptance criteria:
- pipelines/README.md accurately describes exactly what azure-pipelines.yml does.
- azure-pipelines.yml does not perform real deployment by default.
- Any deployment-related stage is clearly named or described as a template/placeholder.
- There is no misleading claim that app deployment is fully implemented.
- Placeholder service connection/variable group values are still clearly marked as placeholders.

Open issue 2: Validation/test runs still leave generated synthetic data and export artifacts in the working tree

Current problem:
`git status -sb` shows modified tracked files such as:
- data/exports/feedback_eval_dataset.json
- data/synthetic/ai_feedback.json
- data/synthetic/assignment_history.json
- data/synthetic/audit_events.json
- data/synthetic/cost_records.json
- data/synthetic/fraud_alerts.json
- data/synthetic/notification_preferences.json
- data/synthetic/notifications.json
- data/synthetic/readiness_assessments.json
- data/synthetic/telemetry_events.json
- frontend/next-env.d.ts

It also shows untracked generated compliance exports like:
- data/exports/compliance/case-001/COMP-*.json

Required fix:
- Ensure targeted validation tests do not mutate repository data.
- Review the existing test isolation fixture in backend/tests/conftest.py.
- Extend or correct it so tests write all mutable data, exports, telemetry, feedback, cost, compliance, readiness, notifications, audit, assignment, and related synthetic files to temporary test paths.
- If frontend build modifies `frontend/next-env.d.ts`, determine whether this file should be committed, ignored, or left as generated framework output. Prefer the standard Next.js project convention used by this repo.
- Do not blindly delete all data files. First identify which changes are generated test/build artifacts.
- Restore or clean only generated artifacts caused by validation, preserving intentional user edits.

Acceptance criteria:
- After running the required validation commands, `git status -sb` does not show new modifications to synthetic data/export files caused by tests.
- Untracked `data/exports/compliance/case-001/COMP-*.json` files are not created by tests.
- Backend targeted tests still pass from repo root and backend directory.
- Frontend build/test/lint still pass.
- Working tree should not have generated validation artifacts beyond intentional source/docs/test changes.

Suggested investigation:
- Search for services/repositories that write to:
  - data/synthetic
  - data/exports
  - compliance_export_path
  - feedback_export_path
  - readiness_report_export_path
  - telemetry_events.json
  - audit_events.json
  - notification files
  - cost_records.json
- Check whether services use settings paths directly or hardcoded paths.
- Patch test fixtures or service configuration so tests isolate these writes.

Validation commands to run:
1. git diff --check
2. From repo root:
   python -m pytest backend\tests\unit\test_local_auth.py backend\tests\integration\test_auth_routes.py backend\tests\unit\test_secure_config_loader.py
3. From backend directory:
   python -m pytest tests\unit\test_local_auth.py tests\integration\test_auth_routes.py tests\unit\test_secure_config_loader.py
4. From frontend directory:
   npm run test
   npm run lint
   npm run build
5. If backend fixture/path code changed:
   python -m compileall app
6. Check:
   git status -sb

Final response should include:
- Exact pipeline consistency changes made.
- Exact test/data isolation changes made.
- Whether generated data artifacts were cleaned/restored.
- Validation results.
- Any remaining open issues.