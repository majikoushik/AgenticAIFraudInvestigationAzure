# Retention Policy Misconfiguration

Disable unsafe policy overrides, reset to defaults if needed, require legal/compliance review, and rerun a dry-run scan before enabling any archive or purge execution.
