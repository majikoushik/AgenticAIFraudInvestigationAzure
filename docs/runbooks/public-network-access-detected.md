# Public Network Access Detected

Confirm production parameters set `disablePublicNetworkAccess=true`, verify each Azure resource supports the setting, and ensure private endpoint/DNS validation passes before disabling public access.
